/*---Generals---*/
npm init -y
npm install express
npm install ejs
npm install method-override
npm install multer
npm install express-validator
npm install express-session
npm install bcryptjs
npm install node-cron
npm install moment-timezone
npm install session-file-store
npm install cors

/*---Sequelize---*/
1. npm install --save sequelize-cli
2. npm install --save sequelize
3. npm install mysql2

/*---excel---*/
npm install exceljs
npm install read-excel-file

Resumed --->
npm init -y
npm install express ejs method-override multer express-validator express-session bcryptjs sequelize-cli sequelize mysql2 exceljs read-excel-file node-cron moment-timezone session-file-store cors
npx sequelize-cli init

/*---POPUPS---*/
CHSPP --> 'Change season popup'








