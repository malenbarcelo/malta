/*---Generals---*/
npm init -y
npm install express
npm install ejs
npm install method-override
npm install multer
npm install express-validator
npm install express-session
npm install bcryptjs
npm install node-cron

/*---Sequelize---*/
1. npm install --save sequelize-cli
2. npm install --save sequelize
3. npm install mysql2

/*---excel---*/
npm install exceljs
npm install read-excel-file

Resumed --->
npm init -y
npm install express ejs method-override multer express-validator express-session bcryptjs sequelize-cli sequelize mysql2 exceljs read-excel-file node-cron
npx sequelize-cli init

/*---SEQUELIZE IMPORTANT!!!---*/
Para poder usar dos bases de datos se hicieron algunos cambios en la configuración de selquelize.

1. Generar archivo sequelizeConfig.js
2. index.js --> copiar la parte del código no comentada
3. archivos dbQueries.js -->
ejemplo:
const db = require('../../../database/models')
const { localDB } = require('../../../database/config/sequelizeConfig')
const sequelize = require('sequelize')
const Orders = db.local.Orders






